<resources>
    <!-- Player theme -->
    <style name="Theme.JellyCast.Player" parent="Theme.JellyCast">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.JellyCast" parent="Base.Theme.JellyCast" />

    <style name="Base.Theme.JellyCast" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">@color/primary_container_light</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container_light</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/secondary_light</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorSecondaryContainer">@color/secondary_container_light</item>
        <item name="colorOnSecondaryContainer">@color/on_secondary_container_light</item>

        <!-- Surface -->
        <item name="android:colorBackground">@color/neutral_50</item>
        <item name="colorSurface">@color/neutral_100</item>
        <item name="colorSurfaceContainer">?attr/colorSurface</item>
        <item name="colorSurfaceContainerLow">?attr/colorSurface</item>
        <item name="colorSurfaceContainerHigh">?attr/colorSurface</item>

        <!-- Error -->
        <item name="colorError">@color/red</item>
        <item name="colorOnError">@android:color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>

        <!-- Shapes -->
        <item name="shapeAppearanceCornerExtraSmall">
            @style/ShapeAppearance.JellyCast.Corner.ExtraSmall
        </item>
        <item name="shapeAppearanceCornerSmall">@style/ShapeAppearance.JellyCast.Corner.Small</item>

        <!-- Toolbar -->
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar.Surface</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">?attr/colorSurfaceContainer</item>
        <item name="android:windowLightNavigationBar">?attr/isLightTheme</item>

        <!-- Extra -->
        <item name="elevationOverlayEnabled">false</item>
        <item name="alertDialogTheme">@style/ThemeOverlay.Material3.MaterialAlertDialog</item>
        <item name="dialogCornerRadius">28dp</item>
    </style>
</resources>